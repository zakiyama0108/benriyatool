---
name: fix
description: バグ修正・既存機能の小規模な改修を行うときに使う(計算誤りの修正、文言変更、既存specのスコープ外だった項目への対応など)。既存spec更新時の確認範囲と承認要否の判断を扱う。新しい機能・画面の追加は/requirementを使う。
---

> ワークフロー上の位置: **/fix(本Skill)** → [/implementation-review](../implementation-review/SKILL.md) → [/pr](../pr/SKILL.md)(実装PR)

# Step1 入口の確認

[/requirement](../requirement/SKILL.md)のStep0「新規spec作成 vs 既存spec更新の判断」に照らして、今回の変更が**既存spec更新**に該当することを確認する。「新規specを作るべきサイン」に該当したら、本Skillではなく/requirementのフローへ切り替える。

# Step2 仕様(3点セット)への影響を洗い出す

既存specを更新する場合、3点セットすべてへの影響を洗い出す。1つのファイルだけ直して終わりにしない。

- **requirements.md**: 該当する見出しの`[n]`を書き換える、または新しい`[n]`を追記する(既存の連番は崩さない)。エッジケース・スコープ外に古い記述が残っていないか確認する
- **design.md**: 処理フローの該当する処理を更新する。処理の呼び名・手順が変わった場合は書き直す
- **tasks.md**: 新しいタスクとして追記する(過去タスクの完了チェックは履歴として残し、変更しない)

さらに、仕様書だけでなく実装・テストへの影響も必ず洗い出す:

- 実装コード(該当する`app/`配下のファイル)のどこを変更する必要があるか
- 既存テストの`// 仕様: specs/.../requirements.md#見出し-n`コメントが指す見出し・`[n]`がずれないか(見出し名や番号を変更したら、対応するテストコメントも合わせて書き換える)
- `npm run check:spec-coverage`を実行し、新旧の対応関係が壊れていないか確認する
- `specs/<アプリ名>/architecture.md`があれば、機能マップ・関連ADR等に影響がないか確認する

## 仕様承認の要否

修正の種類によって承認ゲートの扱いが分かれる:

- **仕様そのものを変える場合**(ビジネスルールの変更、スコープ外だった項目への対応など): 先に3点セットを更新して仕様承認PR([/pr](../pr/SKILL.md))を出し、ユーザーの承認を得てから実装に入る
- **仕様は正しく実装が誤っている場合**(純粋なバグ)や、仕様に影響しない軽微な変更(typo・文言修正): 承認ゲートは不要。そのままStep3へ進む

# Step3 修正(TDD)

`feature/<機能名>`ブランチを切り、まずバグ・問題を再現する失敗テスト(🔴Red)を書いてから修正する(テストの命名・仕様コメントのルールは[/implementation](../implementation/SKILL.md)を参照)。Step2で洗い出した仕様書・テストコメント・`scripts/spec-coverage-skip.json`の追随も同じ変更の中で行う。

# 完了時の次ステップ案内

修正・テスト・動作確認が済んだら、[/implementation-review](../implementation-review/SKILL.md)(コードレビュー)→[/pr](../pr/SKILL.md)(実装PR)へ進むことを案内する。