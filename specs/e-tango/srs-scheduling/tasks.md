> TDDで進める。各タスクは 🔴 Red(失敗するテストを書く) → 🟢 Green(最小実装) → 🔵 Refactor の順で進める。

## T1: 4段階を自動判定する処理
- 🔴 `__tests__/e-tango/lib/gradeAnswer.test.ts`: 不正解→「もう一度」、正解+ヒント使用→「むずかしい」(回答時間によらず)、正解+ヒントなし+閾値未満→「かんたん」、正解+ヒントなし+閾値以上→「ふつう」の4パターンを検証するテストを書く
- 🟢 `app/e-tango/lib/gradeAnswer.ts`を実装する
- 🔵 閾値(秒数)を定数として切り出し、根拠をコメントに残す

## T2: ts-fsrsの導入と次回復習日の計算
- 🔴 `__tests__/e-tango/lib/scheduleReview.test.ts`: 新規カード(履歴なし)に「ふつう」判定を与えた場合に次回復習日が未来日になること、既存カードに「もう一度」を与えた場合に安定度が下がり復習日が近くなること、目標保持率が計算に反映されることを検証するテストを書く
- 🟢 `ts-fsrs`をpackage.jsonに追加し、`app/e-tango/lib/scheduleReview.ts`にCard型変換・スケジューラ呼び出しを実装する
- 🔵 `ts-fsrs`のCard型と`progress-store`保存用の型の変換部分を関数として整理する

## T3: 習得済みを判定する処理
- 🔴 `__tests__/e-tango/lib/scheduleReview.test.ts`(追記): 安定度21日以上で「習得済み」、21日未満で「学習中」になること、手動切替フラグがある場合は自動判定で上書きされないことを検証するテストを書く
- 🟢 習得済み判定関数を実装し、`scheduleReview.ts`から呼び出す
- 🔵 21日という閾値を定数化し根拠をコメントに残す

## T4: 今日のキューを構成する処理
- 🔴 `__tests__/e-tango/lib/buildDailyQueue.test.ts`: 復習対象がすべて含まれること、復習対象が閾値を超えたら新規語が段階的に減ること、さらに超えたら新規語が0になること、出題順(復習日超過→新規語導入)を検証するテストを書く
- 🟢 `app/e-tango/lib/buildDailyQueue.ts`を実装する
- 🔵 新規語を減らす閾値を定数化し根拠をコメントに残す

## T5: セッション内リピートの再出題を差し込む処理
- 🔴 `__tests__/e-tango/lib/sessionRepeat.test.ts`: 新規語が2問以上はさんで再出題されること、連続2回正解で対象から外れること、不正解で連続正解カウントが0に戻り間隔が詰まること、再出題ごとに前回と別パターンになることを検証するテストを書く
- 🟢 `app/e-tango/lib/sessionRepeat.ts`を実装する
- 🔵 セッション内リピートのパラメータ(目標正解回数・最小間隔)を定数化する
