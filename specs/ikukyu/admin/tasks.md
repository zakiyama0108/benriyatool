# タスク: 管理画面(保存データの閲覧・集計)

> TDDで進める。各タスクは 🔴 Red(失敗するテストを書く) → 🟢 Green(最小実装) → 🔵 Refactor の順で進める。

## 事前作業(実装より先に単独PRで適用)

- [ ] T0. 許可リストテーブルと閲覧用RLSポリシーのマイグレーションを作成し、単独PRでmainにマージして適用する(design.md#マイグレーション)。適用後、Supabaseダッシュボードで`admin_emails`に運営者のメールを1件登録する(gitには書かない)
  - このタスクはスキーマ・ポリシー変更のみで、TDDのテスト対象外(spec-coverage-skip.jsonに登録)

## 集計・整形ロジック(純粋関数・TDDの中心)

- [ ] T1. 絞り込み条件を組み立てる(`fetchResults.ts`の条件組み立て部分を純粋関数に切り出す) — 期間の有無・テストデータを含めるかで、取得条件がどう変わるかをテストする(design.md#保存データを絞り込んで取得する処理、requirements.md#絞り込み、requirements.md#テストデータの扱い)
- [ ] T2. 一覧の各列を整形する(`format.ts`) — 日付・金額の表示、育休開始日が未設定のときの空表示をテストする(requirements.md#一覧表示-1)
- [ ] T3. 利用件数の合計と期間推移(日別/月別)を集計する(`aggregate.ts`) — 複数レコードから件数と推移の区切りが正しく出ることをテストする(requirements.md#集計表示-1)
- [ ] T4. モード別の件数と比率を集計する(`aggregate.ts`) — ママ/パパの件数と比率、合計が0件のときの扱いをテストする(requirements.md#集計表示-2、design.md#集計を組み立てる処理の手順5)
- [ ] T5. 月給の平均と5区分の分布を集計する(`aggregate.ts`) — 各区分の境界値(20万・30万・40万・50万ちょうど)がどちらに入るかを含めてテストする(requirements.md#集計表示-3、requirements.md#表示の初期値・区切り-2)
- [ ] T6. 合計給付額・合計取得日数の平均を集計する(`aggregate.ts`) — 平均の算出と、0件時に算出対象なしとなることをテストする(requirements.md#集計表示-4)

## 認証・データ取得(外部依存あり)

- [ ] T7. ログイン開始・ログアウト・セッション取得・閲覧権限の確認(`auth.ts`) — Supabaseクライアントをモックし、セッションの有無・許可リストに載っているかで戻り値が変わることをテストする(design.md#ログイン状態を判定して画面を出し分ける処理、design.md#閲覧権限を確認する処理、requirements.md#ログイン・アクセス制御-1〜4)
- [ ] T8. 絞り込み条件でレコードを取得する(`fetchResults.ts`) — Supabaseクライアントをモックし、T1の条件が取得呼び出しに反映されること・取得失敗時にエラーを呼び出し元へ伝えることをテストする(design.md#保存データを絞り込んで取得する処理、design.md#エラーハンドリング)

## 画面(コンポーネント)

- [ ] T9. ログイン/権限なしの案内画面(`LoginScreen.tsx`) — 未ログイン時にログインボタン、権限なし時に権限がない旨とログアウトが出ることをテストする(requirements.md#ログイン・アクセス制御-1、requirements.md#ログイン・アクセス制御-3)
- [ ] T10. 絞り込み操作(`FilterBar.tsx`) — 期間・テストデータ切替の操作で、変更が呼び出し元に伝わることをテストする(requirements.md#絞り込み)
- [ ] T11. 一覧表(`ResultsTable.tsx`) — レコード配列を渡すと各列と件数が表示されることをテストする(requirements.md#一覧表示)
- [ ] T12. 集計表示(`SummaryStats.tsx`) — 集計結果を渡すと件数・比率・分布・平均が表示されることをテストする(requirements.md#集計表示)
- [ ] T13. 管理画面本体(`page.tsx`)で状態を組み立てる — ログイン状態・権限・絞り込み条件に応じて、案内/エラー/一覧+集計を出し分ける。絞り込み変更で取得し直すつなぎ込み(design.md#状態管理、design.md#エラーハンドリング)

## 仕上げ

- [ ] T14. `specs/ikukyu/architecture.md`の機能マップに`admin`行を追加する(design.md作成時に反映済みなら確認のみ)
- [ ] T15. `specs/legal/requirements.md`のプライバシーポリシー更新要否を確認する(requirements.md#依存関係)
- [ ] T16. requirements.md冒頭の`> ステータス: 仕様確認中(未実装)`行を削除する(実装のテストが仕様項目に紐づいた段階で)
- [ ] T17. `scripts/spec-coverage-skip.json`にスキップ項目を登録する。`check:spec-coverage`はrequirements.md/design.mdの**全項目**を「✅テスト対応 or ⏭スキップ」にすることを要求するため、テストで担保しない項目をすべて登録しないと、T16のWIPマーカー削除後にCIが多数❌で落ちる。登録範囲は以下(既存のsave-result/simulatorの登録運用に倣う):
  - **design.mdの全見出し・表行**: 処理フロー(##)と各処理(###)、エラーハンドリング、関連するファイル(抜粋)、データベース設計、`admin_emails (新規)`表とその行(`admin_emails (新規) > email`)、マイグレーション、画面設計、状態管理、セキュリティ、ログ(design.mdの処理フロー・メタ見出しはテストから参照しない方針のため)
  - **requirements.mdのメタ見出し**: 概要・ユーザーストーリー・依存関係・スコープ外に加え、配下に`###`を持ち直下に`- [n]`が無い**親見出しも項目化される**ため`機能要件`・`ビジネスルール・制約`も登録する(既存save-resultの「ビジネスルール・制約」登録と同じ)
  - **requirements.mdのうちテストで担保できない項目**: RLS/OAuth/運用前提で単体テスト不可の項目(ログイン・アクセス制御[2](OAuth)、アクセス制御・権限[1][2][3]、認証手段とパスキー[1]〜[5]、非機能要件[1][2])。これらの実挙動(実際のRLS・OAuthリダイレクト・実機ログイン)は初回公開前の手動確認で担保する(requirements.md#認証手段とパスキー-5、design.md#マイグレーションの実機確認)
  - **スキップしない(テストで担保する)項目**: ログイン・アクセス制御[1][3][4]、一覧表示[1][2][3]、集計表示[1]〜[4]、絞り込み[1][2]、テストデータの扱い[1][2]、表示の初期値・区切り[1][2][3](期間の初期値・5区分・集計対象の絞り込みはT1〜T6でテストする)。これらはT1〜T13のテストで✅にする
  - 登録後に`npm run check:spec-coverage`を実行し、❌が0件であることを確認する
